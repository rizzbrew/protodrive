{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test src/tests/*.test.ts",
    "build": "esbuild src/vercel.ts --bundle --platform=node --target=node18 --format=esm --outfile=../api/index.js --packages=external --minify"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3",
    "@hono/node-server": "^1.13.1",
    "dotenv": "^17.2.4",
    "hono": "^4.11.4",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0",
    "vercel": "^37.6.2",
    "esbuild": "^0.25.0",
    "pino-pretty": "^13.1.3"
  }
}
